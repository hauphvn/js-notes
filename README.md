# js-notes

## Variables:

- ``let`` limited scope of variable, should not use ``var``

## Function

- Define a func can use `` const``
- Ex:

```
const sum = (a,b) => a + b;
console.log(sum(2,4));
```

-  
